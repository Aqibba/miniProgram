<scroll-view hidden="{{isShowLyric}}" class="lyric-scroll">
  <view class="lyric-panel">
    <block wx:for="{{lyricList}}" wx:key="item">
      <view class="lyric">{{item.lrc}}</view>
    </block>
  </view>
</scroll-view>